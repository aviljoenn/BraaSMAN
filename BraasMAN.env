# ─── BraaSMAN .env ───────────────────────────────
# Do NOT commit this file to version control!
# Only edit the values—leave the keys as-is.
# Save this file in the project root (same directory as pyproject.toml) and make sure .gitignore contains .env so these secrets stay local.

MT_USER=Admin
MT_PASS=G1j1m@01!
FG_API_TOKEN=x85k4zychghhh7htzjb76Gpkj1zmxf
